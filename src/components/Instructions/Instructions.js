import React, {Component} from "react";
import "./Instructions.css";

const Instructions = () => {
	return(
		<div className="instructions">
			<h4>Do not click on the same image more than once</h4>
		</div>
		)
};

export default Instructions;
